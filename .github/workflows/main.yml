name: Build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: python:3.12

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: pip3 install -r app/app-requirements.txt black pylint pytest pytest-cov coveralls

      - name: Formatter
        run: black --config pyproject.toml app tests --check

      - name: Linter
        run: pylint --rcfile pyproject.toml app tests
        env:
          DATABASE_URI: ${{ secrets.DATABASE_URI }}

      - name: Test
        run: pytest
        env:
          DATABASE_URI: ${{ secrets.DATABASE_URI }}

      - name: Coverage Upload
        run: |
          git config --global --add safe.directory /__w/Get-In-The-Mix-Resumes/Get-In-The-Mix-Resumes
          coveralls --rcfile=pyproject.toml --service=github
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
